% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_pointnet.R
\name{explain_pointnet}
\alias{explain_pointnet}
\title{Explain a trained PointNet model using SHAP or gradient-based methods}
\usage{
explain_pointnet(
  model,
  voxel_data,
  method = c("shap", "gradients"),
  n_samples = 500,
  device = if (torch::cuda_is_available()) "cuda" else "cpu"
)
}
\arguments{
\item{model}{A trained PointNet model (see ecoPointXAI::train_pointnet()).}

\item{voxel_data}{A data frame or torch tensor of voxel features (e.g., X, Y, Z, value)}

\item{method}{One of "shap" (model-agnostic) or "gradients" (model-specific)}

\item{n_samples}{Number of random samples for SHAP (default = 500)}

\item{device}{Computation device: "cpu" or "cuda" (auto-detected)}
}
\value{
A list containing:
\itemize{
\item feature_importance - tibble of average importance per feature
\item local_explanations - matrix of per-voxel attributions (for gradients)
\item shap_values - data frame of Shapley values (for SHAP)
}
}
\description{
Generates voxel-level and feature-level explanations for trained models.
}
