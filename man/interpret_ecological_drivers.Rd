% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret_ecology.R
\name{interpret_ecological_drivers}
\alias{interpret_ecological_drivers}
\title{Interpret model explainability in ecological terms}
\usage{
interpret_ecological_drivers(shap_data, eco_metrics, top_n = 10, plot = TRUE)
}
\arguments{
\item{shap_data}{A data.frame with at least X_grid, Y_grid, and importance columns.}

\item{eco_metrics}{Output object from \code{\link[=compute_ecological_metrics]{compute_ecological_metrics()}}.}

\item{top_n}{Optional integer (default 10) - number of top drivers to report.}

\item{plot}{Logical (default TRUE) - whether to visualize correlations.}
}
\value{
A list with correlation table and textual interpretation.
}
\description{
Correlates feature importance values (e.g., SHAP) with canopy metrics
derived from voxel data, generating both quantitative and textual insights.
}
\examples{
# Example using synthetic voxel data
vox <- data.frame(
  X = runif(100, 0, 10),
  Y = runif(100, 0, 10),
  Z = runif(100, 0, 5),
  value = runif(100)
)
metrics <- compute_ecological_metrics(vox)

shap_data <- data.frame(
  X_grid = metrics$grid_metrics$X_grid,
  Y_grid = metrics$grid_metrics$Y_grid,
  importance = runif(nrow(metrics$grid_metrics))
)

interpret_ecological_drivers(shap_data, metrics)
}
