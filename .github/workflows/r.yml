name: R-CMD-check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  R-CMD-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 🧠 Set up R (Ubuntu binary)
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          install-from: binary

      - name: 📦 Install dependencies
        run: |
          Rscript -e 'install.packages("remotes", repos="https://cloud.r-project.org")'
          Rscript -e 'remotes::install_deps(dependencies = TRUE)'
          Rscript -e 'install.packages("rcmdcheck", repos="https://cloud.r-project.org")'

      - name: ✅ Run R CMD check
        run: |
          Rscript -e 'if (!requireNamespace("rcmdcheck", quietly = TRUE)) install.packages("rcmdcheck", repos="https://cloud.r-project.org"); rcmdcheck::rcmdcheck(args = "--no-manual", error_on = "warning")'
